import speech_recognition as sr
import pyttsx3
import datetime
import webbrowser

recognizer = sr.Recognizer()
engine = pyttsx3.init()

engine.setProperty("rate", 175)

def speak(text):
    print("SPIDEY:", text)
    engine.say(text)
    engine.runAndWait()

def listen():
    with sr.Microphone() as source:
        print("Listening...")
        recognizer.adjust_for_ambient_noise(source, duration=0.5)
        audio = recognizer.listen(source)

    try:
        command = recognizer.recognize_google(audio)
        print("YOU:", command)
        return command.lower()
    except:
        return ""

def run_spidey():
    speak("Spidey online. What's up?")

    while True:
        command = listen()

        if "time" in command:
            time = datetime.datetime.now().strftime("%I:%M %p")
            speak("The time is " + time)

        elif "youtube" in command:
            speak("Opening YouTube.")
            webbrowser.open("https://youtube.com")

        elif "google" in command:
            speak("Opening Google.")
            webbrowser.open("https://google.com")

        elif "spider sense" in command:
            speak("Spider sense activated. Something feels suspicious.")

        elif "hello" in command or "hi spidey" in command:
            speak("Hey! Your friendly neighborhood assistant is here.")

        elif "stop" in command or "goodbye" in command:
            speak("Spidey going offline.")
            break

        elif command != "":
            speak("I heard you, but I don't know that command yet.")

run_spidey()
print("🕷️ SPIDEY ASSISTANT 🕷️")
print("Spidey online! What's up?")

while True:
    command = input("You: ").lower()

    if "hello" in command:
        print("Spidey: Hey! Your friendly neighborhood assistant is here 🕷️")

    elif "who are you" in command:
        print("Spidey: I'm Spidey, your personal assistant!")

    elif "spider sense" in command:
        print("Spidey: ⚠️ SPIDER-SENSE ACTIVATED! ⚠️")

    elif "goodbye" in command:
        print("Spidey: See ya! Spidey going offline 🕷️")
        break

    else:
        print("Spidey: Hmm... I don't know that one yet.")
